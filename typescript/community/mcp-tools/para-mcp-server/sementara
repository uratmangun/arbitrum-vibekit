@typescript/community/mcp-tools/para-mcp-server/src/tools make a new tool that create a raw transaction for transferring eth to some evm chain try to use @docs:viem to do that and then return it to look like this JSON string @message.renderer.tsx#L24-50 so in order to understand why i ask you to do that is because i want to trigger this component to show in the chat@TemplateComponent.tsx#L59-158 which is why i need the JSON to be specific like that @message.renderer.tsx#L157 it will be triggered in here so please get me those txPreview
